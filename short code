  // To give color from code through using (RGB)
  backgroundColor = #colorLiteral(red: 0.4588235294, green: 0.7568627451, blue: 0.7725490196, alpha: 1)

  // Waiting for new member here ;)


//for alert message
//Extention funcction banaya ye
   func showCommonAlert(message : String) {
          let alert = UIAlertController(title: "Quippy", message: message, preferredStyle: .alert)
          let okButton = UIAlertAction(title: "OK", style: .default)
          alert.addAction(okButton)
          self.present(alert, animated: true)
      }
//then Button pe call karaya ye
      self.showCommonAlert(message: "Under Progress....")
          print("Term and Conditions")
//MARK: - Set TimerInterval on any task
 DispatchQueue.main.asyncAfter(deadline: .now() + 5.0, execute: {
            self.pushToHomeVcBtnOlt.isEnabled = true   // code which need to execute
        })
